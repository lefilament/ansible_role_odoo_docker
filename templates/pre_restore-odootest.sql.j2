REVOKE CONNECT ON DATABASE {{ item.value.db }} FROM public;
SELECT pg_terminate_backend(pg_stat_activity.pid) FROM pg_stat_activity WHERE pg_stat_activity.datname = '{{ item.value.db }}';
DROP DATABASE {{ item.value.db }};
